return {
  'rebelot/kanagawa.nvim',
  config = function()
    require('kanagawa').setup {
      colors = {
        theme = {
          all = {
            ui = {
              bg_gutter = 'none',
            },
          },
        },
      },

      background = {
        -- dark = 'dragon',
        dark = 'wave',
        light = 'lotus',
      },
    }
    vim.cmd.colorscheme 'kanagawa'
  end,
}
